name: inlet
metadata:
  template: inlet
services:
  relayer:
    project: ./services/relayer
    host: containerapp
    language: ts
    docker:
      path: Dockerfile
      context: ../..
      remoteBuild: true
